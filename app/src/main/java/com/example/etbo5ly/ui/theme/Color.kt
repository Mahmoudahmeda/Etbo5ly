package com.example.etbo5ly.ui.theme

import androidx.compose.ui.graphics.Color

// Default Compose colors
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

//-----------------------------------------------------------

// App bar gradient (Old Palette - keep or remove if not used)
val AppBarColor = Color(0xFF51FBFB)   // top color
val AppBarColorShade = Color(0xFF309595)    // shaded down

// Text
val AppBarText = Color.Black
val ProductTitle = Color.White
val Subtitle = Color(0xFFEDEDED)

// Backgrounds
val BackgroundDark = Color(0xFF171A1F)
val CardBottom = Color(0xFF383838)

// Frames / borders
val FrameGray = Color(0xFF626262)


// Dark Mode Palette
val Turquoise = Color(0xFF2EC4B6)      // Main action color for dark mode (buttons, icons, top of app bar gradient)
val TurquoiseShade = Color(0xFF1B857A) // Darker version of turquoise, used for the bottom of app bar gradients in dark mode
val MidnightBlue = Color(0xFF1A1A2E)   // Deep dark background for screens in dark mode
val MidnightBlueSurface = Color(0xFF252545) // Slightly lighter blue for cards/surfaces in dark mode
val LighterBlueSurface = Color(0xFF252545) // Lighter blue for TextFields/Unselected buttons

// Light Mode Palette
val DeepRed = Color(0xFFD62828)        // Main brand color for light mode (Top bars, primary buttons, top of app bar gradient)
val DeepRedShade = Color(0xFFA01E1E)   // Darker version of deep red, used for the bottom of app bar gradients in light mode
val WarmCream = Color(0xFFFFF3E0)      // Soft, warm background color for light mode
val WarmCreamSurface = Color.White      // Clean white for cards in light mode

// General UI colors
val AppBarTextDark = Color.Black       // Text color for app bars in dark mode
val AppBarTextLight = Color.White      // Text color for app bars in light mode (since red is dark)
val SubtitleGray = Color(0xFF8E8E93)   // Muted text for descriptions and hints
val BorderGray = Color(0xFFE0E0E0)     // Subtle dividers and outlines
